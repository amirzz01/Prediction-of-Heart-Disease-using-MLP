# -*- coding: utf-8 -*-
"""HeartDisease.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1cAXQjMo7rJoSqixifFHTZP_siRE-IU8A
"""

import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score

from google.colab import files

uploaded = files.upload() #dataset upload local

file = pd.read_csv('دیتا بیماران قلبی.csv')

file.head() #showing first 5 rows of dataset

print(file.shape) #showing rows and columns of dataset

file.info() #showing if any data contains null variable

file.describe() #some useful info about variables values

# Create a plot of crosstab
pd.crosstab(file.output, file.sex).plot(kind="bar",color=["red", "blue"])
plt.title("Heart Disease Frequency for Sex")
plt.xlabel("0 = No Disease, 1 = Disease")
plt.ylabel("Amount")
plt.legend(["Female", "Male"]);
plt.xticks(rotation=0);

# Make the crosstab more visual
pd.crosstab(file.cp, file.output).plot(kind="bar", color=["red", "blue"])


plt.title("Heart Disease Frequency Per Chest Pain Type")
plt.xlabel("Chest Pain Type")
plt.ylabel("Amount")
plt.legend(["No Disease", "Disease"])
plt.xticks(rotation=0);

file['output'].value_counts() #from all 303 subjects showing how many people have heart disease and how many people doesnt

X = file.drop('output', axis = 1)
Y = file['output']
X.head() #seperating feautres and result, as in this dataset result is output we seperate that column from other data

X_train, X_test, Y_train, Y_test = train_test_split(X, Y, test_size = 0.2) #splitting data to train and test, 20% goes to test and 80% train

print(X.shape, X_train.shape, X_test.shape) #showing original data, train and test count

model = LogisticRegression() #using logistic regression to train the model

model.fit(X_train, Y_train)

X_test_prediction = model.predict(X_test)
test_accuracy = accuracy_score(X_test_prediction, Y_test) #this part tests the data splitted as test to see how accurate we can predict the heart disease

print('Accuracy on data is:',test_accuracy)

print('Enter following details to predict whether someone is more likely to have Heart Disease or not: ')
a = int(input('age : '))
s = int(input('sex [Male:0, Female:1]: '))
c  = int(input('cp [0,1,2,3]: '))
tr = int(input('trestbps [80 to 200]: '))
ch = int(input('chol [90 to 580]: '))
f = int(input('fbs [0,1]: '))
r = int(input('restecg [0,1]: '))
t = int(input('thalach [60 to 210]: '))
e = int(input('exang [0,1]: '))
o = float(input('oldpeak [0 to 7]: '))
sl = int(input('slope [0,1,2]: '))
ca = int(input('ca [0,1,2]: '))
th = int(input('thal [0,1,2,3]: ')) #taking data to use them

input_data = (a,s,c,tr,ch,f,r,t,e,o,sl,ca,th) #puting data we got in last cell
input_array = np.asarray(input_data) #making the data an numpy array
input_data_reshaped = input_array.reshape(1,-1) #reshape the array so we only proccess this part not all data
prediction = model.predict(input_data_reshaped) #predict if person is sick or not based on data

if (prediction == 0):isill = False
elif(prediction == 1):isill = True
print('Likely sick' if isill == True else 'Not sick:)' )